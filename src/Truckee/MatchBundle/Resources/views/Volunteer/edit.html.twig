{% extends 'base.html.twig' %}

{% block body %}
    <form action=""  method="POST"  class="form-inline">
        {{ form_widget(form._token) }}
        {% if edit is defined %}
            {{ form_row(form.receiveEmail) }}
        {% endif %}
        {#        #}
        {% include "TruckeeMatchBundle:Volunteer:volunteer_content.html.twig" %}
    </form>
{% endblock %}
