{% extends 'base.html.twig' %}
{% block body %}
    {# Following 3 lines adds volunteer registration link for anonymous users #}
    {% for template in templates %}
        {% include template %}
    {% endfor %}
    <a class="btn btn-primary btn-xs active" href="{{ path("opp_search") }}">Search for opportunities</a>

{% endblock body %}
