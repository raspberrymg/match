{% extends 'base.html.twig' %}
{% block body %}
    <div class="row">
        <div class="col-md-4">
            <h4>{{ label_info('Event') }}</h4>
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
